query LaunchList {
    launches {
        cursor
        hasMore
        launches {
            id
            site
            ... missionFragment
        }
    }
}

fragment missionFragment on Launch {
    mission {
        name
        missionPatch ( size: SMALL)
    }
}

subscription TripsBooked {
    tripsBooked
}
